services:
  postgres:
    image: postgres:16
    container_name: restaurant-inventory-postgres
    environment:
      POSTGRES_DB: restaurant_inventory
      POSTGRES_USER: restaurant_inventory_user
      POSTGRES_PASSWORD: restaurant_inventory_password
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
